# hopai.json_api

The JSON front end — same keys as Python, JSON in and out — and the ready-made LLM tool schemas. See [The JSON interface](../reference/json-interface.md) for the guide.

::: hopai.json_api
    options:
      members: [traverse_json, spec_to_traversal, TRAVERSE_TOOL_SCHEMA, aggregate_json, spec_to_aggregation, AGGREGATE_TOOL_SCHEMA, vector_search_json, VECTOR_SEARCH_TOOL_SCHEMA, RerankPolicy, parse_rerank]
