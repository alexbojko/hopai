# hopai.schema

The graph-schema notations, the annotation mapping, and what enforcement compiles to. See [Graph schema](../reference/graph-schema.md) for the guide.

::: hopai.schema
    options:
      members: [GraphSchema, NodeType, EdgeType, Property, InferenceReport, TypeConflict, DroppedEdgeType, SchemaViolations]
