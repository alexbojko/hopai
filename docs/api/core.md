# hopai.core

Local paths, split queries, edge-derived nodes, last-hop-only `optional`. See [Schema](../reference/schema.md), [Many graphs, one database](../reference/multi-graph.md) and [Traversal](../reference/traversal.md) for the guide.

::: hopai.core
    options:
      members: [Graph, Subgraph]
