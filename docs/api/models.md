# hopai.models

The DDL, the typed-columns / JSONB split, the composite FK. See [Schema](../reference/schema.md) for the guide.

::: hopai.models
    options:
      members: [Node, Edge, GraphRegistry]
