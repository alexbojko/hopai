# hopai.mutate

What `where=` selects, why a blank filter refuses, the three update semantics and what `detach` does. See [Mutations](../reference/mutations.md) for the guide.

::: hopai.mutate
    options:
      members: [MutationResult, MUTATE_TOOL_SCHEMA, spec_to_mutations]
