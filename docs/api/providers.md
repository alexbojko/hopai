# hopai.providers

Building an embedding client from a provider name and environment variables, for the
two servers that are started from a command line rather than from Python.

::: hopai.providers
    options:
      members: [embedder_from_env, provider_names, describe, ProviderError]
