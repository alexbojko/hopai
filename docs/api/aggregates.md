# hopai.aggregates

The aggregation DSL in both forms, the three aggregation semantics, numeric edge cases. See [Aggregation](../reference/aggregation.md) for the guide.

::: hopai.aggregates
    options:
      members: [Count, Sum, Avg, Min, Max, parse_aggregate]
