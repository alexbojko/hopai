# hopai.filters

The filter DSL in both forms, and why `OR`/`AND`/`NOT` are explicit classes. See [Filters](../reference/filters.md) for the guide.

::: hopai.filters
    options:
      members: [AND, OR, NOT, GT, GTE, LT, LTE, BETWEEN, parse_filter]
