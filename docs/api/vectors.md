# hopai.vectors

Why no pgvector, the storage and cost model, cosine-only, multivector
semantics, and why a vector never passes through a tool schema. See
[Vector search](../reference/vector-search.md) for the guide.

::: hopai.vectors
    options:
      members: [Vector, Near, Boost, parse_near, parse_boost]
