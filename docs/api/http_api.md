# hopai.http_api

The Python entry point behind the `hopai-api` command — the endpoints, the permission
gate and the explorer page are in [the guide](../http-api.md).

::: hopai.http_api
    options:
      members: [build_app, serve, Served]
